# macOS
.DS_Store
Thumbs.db

# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.log
*.out
*.synctex.gz
*.fls
*.fdb_latexmk
*.toc
*.nav
*.snm
*.vrb

# Editor / OS
*.swp
*.swo
*~

# Node (in case anyone ever runs an experiment)
node_modules/

# Vercel
.vercel/

# Env / secrets (defense in depth — no .env planned for v1)
.env
.env.*

# Build artifacts (none expected, but safe to ignore)
dist/
build/

# GSD planning artifacts — kept locally, never published to the public repo.
# Contains audit-trail collaborator names (D-13 internal-only) and sundry workflow state.
.planning/

# Personal photo originals — kept locally for re-export, not committed to public repo.
# The deploy-quality compressed copies live under assets/img/ and ARE committed.
_src/hero/

# Personal course material — not part of the website
18821S2026 Course info-1.pdf
