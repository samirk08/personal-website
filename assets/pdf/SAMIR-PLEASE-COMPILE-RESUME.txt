Samir — please compile the resume PDF before deploy.

Status (Plan 02-01, 2026-05-07):
  pdflatex is installed (BasicTeX 2025, TeX Live), but several standard
  packages required by resume.tex are NOT in BasicTeX and could not be
  installed by the executor agent because tlmgr requires admin privileges
  to write into /usr/local/texlive/2025basic/.

Missing packages (first failure: fullpage.sty; full set per resume.tex
preamble): preprint (provides fullpage.sty), titlesec, marvosym, enumitem,
fancyhdr, babel-english, tabularx.

Manual fix (one-time, ~2 minutes):

  # Option A — install the missing packages into your existing BasicTeX:
  sudo tlmgr update --self
  sudo tlmgr install preprint titlesec marvosym enumitem fancyhdr babel-english tabularx

  # Option B — install MacTeX (full distribution, ~5 GB) which has everything:
  brew install --cask mactex-no-gui

Then compile from the repo root:

  pdflatex -interaction=nonstopmode -halt-on-error resume.tex
  mv resume.pdf assets/pdf/samir-kadariya-resume.pdf
  rm -f resume.aux resume.log resume.out resume.fdb_latexmk resume.fls resume.synctex.gz
  rm -f assets/pdf/SAMIR-PLEASE-COMPILE-RESUME.txt
  git add assets/pdf/samir-kadariya-resume.pdf
  git rm assets/pdf/SAMIR-PLEASE-COMPILE-RESUME.txt 2>/dev/null || true
  git commit -m "feat(02-01): compile resume PDF (Samir's manual completion)"

After this, /assets/pdf/samir-kadariya-resume.pdf will resolve and
the homepage Resume CTA (CV-01) will work in production. Phase 5
verification audit will fail-loud if the PDF is still missing at
deploy time, so this is a tracked gap, not silent.

Plan 02-01 reference: assets/pdf/samir-kadariya-resume.pdf is the
ONLY allowed PDF link from the homepage (per D-06 + UI-SPEC §Resume
CTA). The link target stays /assets/pdf/samir-kadariya-resume.pdf
regardless of fallback state — the file just needs to exist.
